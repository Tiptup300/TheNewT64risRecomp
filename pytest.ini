[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v -ra --tb=short
# Each game boot + navigation can take ~20-40s under Dozen; keep generous timeouts
# inside the tests themselves (the harness uses select()-based waits).
